<h1>Edit Wish</h1>

<%= form_with(model: @wish, local: true) do |form| %>
  <% if @wish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wish.errors.count, "error") %> prohibited this wish from being saved:</h2>

      <ul>
        <% @wish.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Название %>
    <%= form.text_field :Название %>
  </div>

  <div class="field">
    <%= form.label :Описание %>
    <%= form.text_area :Описание %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= link_to 'Назад', profile_user_path(@current_user) %>
